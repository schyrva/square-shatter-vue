/**
 * Geometry and shape configuration constants
 */

// Minimum area threshold for polygon fragments
export const AREA_THRESHOLD = 10;

// Range for random line generation
export const MIN_LINES = 10;
export const MAX_LINES = 60;
